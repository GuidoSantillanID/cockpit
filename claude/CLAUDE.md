
## Working Relationship

- If my approach is wrong, say so directly. Challenge flawed reasoning, but don't disagree for the sake of it.
- Don't provide timeline estimates or predictions for tasks.
- Flag uncertainty explicitly: distinguish "confident" vs "best guess, thin evidence" vs "speculating."
- When there's a tradeoff, present options with evidence. Don't silently pick the easy path.
- When reviewing code, actively look for problems rather than validating the approach.

## Output Style

Be concise. No narration, no phase announcements ("Now I will verify..."), no filler.

- Lead with the result or action, not the reasoning.
- VERBOSE (bad): "Now I need to check if the imports are correct. Let me verify..."
- CONCISE (good): "Imports: check stdlib, add missing"

## Plan Mode

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any.

## Committing & Attribution

- Never perform any git operation that creates or modifies history — commit, amend, squash, rebase, cherry-pick, reset — without asking for explicit approval first. This applies in all modes.
- You may suggest when it seems like a good time to commit, but always wait for my approval before executing.
- Never claim credit or responsibility for any work. No "Co-Authored-By", "written by Claude", "generated by AI", or similar attribution in commits, PR descriptions, code comments, or anywhere else.

## ESLint

- Never disable ESLint rules with `eslint-disable` comments as a quick fix. Instead, research the rule's intent and find a correct solution (fix the code, adjust config with justification, or consult docs).
- If a rule genuinely needs to be disabled (e.g., false positive, intentional pattern), prompt the user first with reasoning and documentation before adding any `eslint-disable` comment.

## .npmrc auth tokens

Never use `${ENV_VAR}` substitution in a committed `.npmrc` for auth tokens. pnpm warns on every invocation when the var is unset — including non-install commands like `typecheck` and `lint` — and env propagation through turbo/monorepo child processes is unreliable. Instead: have CI append the auth line at runtime (e.g. `echo '//registry/:_authToken=...' >> .npmrc`); have local devs configure once via `pnpm config set //registry/:_authToken <token>` (writes to `~/.npmrc`).

## File operations

Never derive file write paths from exploration results — always use the session's primary working directory as the base. Example: if you discover a file at `/foo/bar/utils.ts`, don't write new files relative to `/foo/bar/`. Resolve paths from the project root.

## wt worktrees

When `.wt-meta` is present in the repo root, this is a worktree managed by the `wt` CLI. Never run `wt` commands via Bash tool — always tell the user to run them in their terminal:

- `wt finish` — rebase onto base branch, fast-forward, remove worktree + branch
- `wt drop` — abandon worktree without merging, remove worktree + branch
- `wt new` — create new worktree and cd into it
- `wt sync` — rebase onto latest base branch from origin
- `wt update` — merge local base branch into worktree; LLM resolves conflicts autonomously where clear, asks user when product/business knowledge is needed. You MAY run this via Bash tool.
- `wt list` — list active worktrees
- `wt doctor` — check worktree health

## TDD — MANDATORY

**This is non-negotiable. Do not skip, defer, or rationalize around it.**

When the user asks to implement a feature or fix a bug using TDD (or when a project's CLAUDE.md specifies TDD):

1. **Write the failing test FIRST.** No implementation code until a test exists and fails.
2. **Run the test to confirm it fails.** Do not assume it fails — execute it.
3. **Only then write the minimum implementation** to make the test pass.
4. **Run the test again to confirm it passes.**

Common violations to avoid:
- "Let me write the implementation and then add tests" — NO.
- "I'll sketch the implementation to understand the shape, then write tests" — NO.
- Writing a test that already passes — that's not TDD, rewrite it.
- Skipping the red-green cycle "for speed" — never.

## When things fail

- Report the failure immediately — don't silently retry.
- Diagnose before acting: show what went wrong, propose a fix, wait for approval on destructive recovery steps.
- If a command fails twice with the same error, stop and ask — don't loop.

## TypeScript

- Never add ambient type declarations (`declare module "*.png"`, etc.) that the framework already provides (e.g., Next.js via `next-env.d.ts`). Duplicate ambient declarations cause non-deterministic errors across OS filesystems (macOS vs Linux process files in different order).
- In Next.js projects, always commit `next-env.d.ts` and remove it from `.gitignore`. It is generated by `next dev` locally but missing on CI when `tsc` runs standalone, causing TS2307 errors. Also add `**/next-env.d.ts` to the root ESLint config's global ignores — it uses triple-slash refs intentionally and must not be linted.